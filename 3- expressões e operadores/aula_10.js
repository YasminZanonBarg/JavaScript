// Operador de String (String operator)

// comparison (comparação, já conhecemos)
console.log ('a' == 'a')

// concatenation (concatenação ou união dos valores)
// Retorna a união de duas Strings
let alpha = 'alpha'
console.log( alpha + 'bet' + 's')

// outro jeito de fazer:

let alpha = 'alpha'
alpha += 'bet'
console.log(alpha)