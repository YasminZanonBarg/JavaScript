// It´s a type of structural data
/* fuction has only two reason, at least so far (até agora):
    1= explain what a piece of code is doing
    2= Repeat the code how many time i want
*/

// Example: Create a motivacional phrase app

// Declaration - declaração de função, can be also be calleda FUNCTION STATEMENT
function createPhrases () {
    console.log('Estudar é muito bom')
    console.log('Paciência e persistência')
    console.log('Revisão é a mãe do aprendizado')
} // if i put just this in my code, when i run(executo) on the console, nothing will appear

// so we need: executar (rodar, chamar, invocar) a função or in english execute, run, invoke, call the function
createPhrases()

console.log('fim do programa')


/*RESUMO: PRA QUE SERVE A FUNÇÃO? 
        - AGRUPAMENTO DE CÓDIGOS
        - REUTILIZAÇÃO DESSE CÓDIGO, QUANTAS VEZES EU QUISER
        - DAR SIGNIFICADO (POSSO DAR UM NOME, QUE VAI DAR SIGNIFICADO AO UM BLOCO DE CÓDIGO)
*/