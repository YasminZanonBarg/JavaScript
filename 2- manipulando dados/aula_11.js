// Manipulando Arrays

// Transfromar uma cadeia de caracteres(string) em elementos de um array

let word = "manipulação"
console.log(Array.from(word))

// cada palavra agora, virou um elemento do array